* Regression testing - this can be done fairly quickly, need to test various functions of the
  app with pre-generated inputs. May also simulate adding/removing options to config file
  (projectconfig_info)

* Making the app more robust - as of now, there is no code to ensure a secure, error-resilient
  workflow. There is no syntax check and verification of any pending changes is the human 
  operator's task

